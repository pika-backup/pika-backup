[package]
name = "pika-backup"
description = "Simple backups based on borg"
version = "0.4.0-alpha"
authors = ["Sophie Herold <sophie@hemio.de>"]
homepage = "https://apps.gnome.org/app/org.gnome.World.PikaBackup/"
repository = "https://gitlab.gnome.org/World/pika-backup"
edition = "2018"
default-run = "pika-backup"

[profile.release]
lto = true
codegen-units = 1

[dependencies]
async-executor = "1.4"
async-process = "1.0"
async-recursion = "0.3"
async-std = "1.10"
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
futures = "0.3.12"
gettext-rs = { version = "0.7", features = ["gettext-system"] }
nix = "0.23"
num-format = "0.4"
regex = "1.4"
shell-words = "1.0"
tempfile = "3.2"
url = "2.2"
walkdir = "2.3"
zxcvbn = "2.0"

# data types and handling
arc-swap = "1.0"
enclose = "1.1"
itertools = "0.10"
once_cell = "1.3"
quick-error = "2.0"
zeroize = "1.1"

# serde
serde = "1.0"
serde_derive = "1.0"
serde_json = "1.0"

# GNOME
gdk-pixbuf = "*"
gio = { version = "*", features = ["v2_54"] }
gio-sys = "*"
glib = "*"
gtk = { package = "gtk4", version = "0.3", features = ["v4_2"] }
pango = "*"

ashpd = "0.2.0-alpha-5"
adw = { version = "0.1.0-alpha-6", package = "libadwaita", git = "https://gitlab.gnome.org/World/Rust/libadwaita-rs.git" }
secret-service = "2.0"
zbus = "2.0.0-beta.7"

[dev-dependencies]
matches = "0.1"
